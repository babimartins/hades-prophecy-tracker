{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // `spacing.test.ts` reads the component sources through `import.meta.glob`
    // rather than through `node:fs`, because it runs in the browser project.
    // That API is Vite's, and its type lives here.
    "types": ["vite/client"]
  },
  "include": ["src", "test"]
}
